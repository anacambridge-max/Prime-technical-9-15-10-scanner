name: Market Scan

on:
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger one live scanner cycle
        run: |
          curl --fail-with-body --retry 2 --max-time 55 \
            "https://prime-technical-9-15-10-scanner2.vercel.app/api/scan?force=1"
